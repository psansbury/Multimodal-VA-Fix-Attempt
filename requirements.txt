# --- Core DL stack (CUDA 12.1 compatible wheels; ensure GPU in WSL) ---
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2

# --- ONNX ecosystem (avoid source builds; protobuf range compatible with ONNX 1.15) ---
onnx==1.15.0
protobuf>=3.20,<5

# --- Notebook / dev UX (pin to stable wheel builds) ---
jupyterlab==3.6.8

# --- Web serving & UI ---
uvicorn[standard]==0.30.0
gradio==4.36.1

# --- LangChain & friends (aligned minor versions to reduce conflicts) ---
langchain==0.2.16
langchain-core==0.2.34
langchain-community==0.2.12
langchain-text-splitters==0.2.2
langgraph==0.2.29

# If you need NVIDIA endpoints integration (ensure account/keys configured):
# langchain-nvidia-ai-endpoints==0.3.19

# --- Multimedia / audio ---
SpeechRecognition==3.10.0
pytubefix==7.1.0
moviepy==1.0.3

# --- NLP, utilities ---
nltk==3.8.1

# --- LlamaIndex (keep pinned or comment if unused) ---
llama-index==0.11.6
# llama-index-vector-stores-lancedb==0.2.2
# llama-index-multi-modal-llms-openai==0.2.0
# llama-index-embeddings-clip==0.2.0
# ftfy==6.2.3
# regex==2024.7.24
# tqdm==4.66.5
